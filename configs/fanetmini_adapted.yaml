# Adapted-track recipe applied to FANetMini (3-scale, TEMImageNet-sized).
# Recipe lives in adapted_recipe.yaml; this file only selects the model.

extends: adapted_recipe.yaml

model:
  name: fanetmini
  # channels and bottleneck are ignored when name == fanetmini (the class
  # hardcodes them); omitted rather than left in as misleading.
